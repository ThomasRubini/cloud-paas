{
    "users": [
        {
            "username": "${OIDC_USER_ID}",
            "enabled": true,
            "credentials": [
                {
                    "type": "password",
                    "value": "${OIDC_USER_PASSWORD}",
                    "temporary": false
                }
            ],
            "clientRoles": {
                "master-realm": [
                    "manage-users"
                ]
            }
        }
    ]
}
